import 'package:flutter/material.dart';

class QuestionAndQuiz extends StatelessWidget {
  const QuestionAndQuiz({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}